# Open Questions

- What exact `gbrain + graphify` export schema should be committed, if any?
- Which memory files should be human-authored source of truth, and which should be generated?
- Should graph memory include runtime observations from JSONL analysis, or only architecture/project knowledge?
- How will generated memory be validated so stale graph facts do not override current code and docs?
